const fs = require('fs');

let content = fs.readFileSync('STATE.md', 'utf-8');
content = content.replace('Интегрирована динамическая генерация изображений (YandexART)', 'Интегрирована динамическая генерация изображений (fal.ai)');
content = content.replace('Исправлена критическая ошибка генерации референсных изображений через YandexART: превышение лимита в 500 символов на длину промпта приводило к сбою API. Добавлено жесткое усечение (`prompt.substring(0, 490)`) для обеспечения стабильной работы.', 'Перешли на fal.ai для библиотеки стрижек по запросу пользователя, так как качество визуальных изображений там выше.');
fs.writeFileSync('STATE.md', content);
