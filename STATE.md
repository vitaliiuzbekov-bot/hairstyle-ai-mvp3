# Бортовой журнал / Состояние проекта

**Текущая задача (04 Июля 2026):**
- Интегрирована динамическая генерация изображений (fal.ai) для библиотеки стрижек
- Перешли на fal.ai для библиотеки стрижек по запросу пользователя, так как качество визуальных изображений там выше. (`HaircutList`) с ленивой загрузкой через `IntersectionObserver`. Это решает проблему случайных заглушек из picsum/unsplash.
- Изображения До/После в модальном окне `BuyModal` заменены на сгенерированные ИИ тематические референсы вместо случайных.
- Исправлена маршрутизация `/api/reference` в `LazyImage.tsx` и `server.ts` для корректной работы middleware.
- Проведен глубокий рефакторинг библиотеки стрижек. Данные вынесены в `src/data/haircutLibrary.ts`. Библиотека значительно расширена современными трендами (Wolf Cut, Pixie, Edgar, Buzz Cut, Hime Cut и др.) и добавлена фильтрация по длине волос и стилю (Короткие, Средние, Длинные, Креатив) в `HaircutList.tsx`.
- Проведена оптимизация рендеринга React (useCallback для коллбэков) и настроено агрессивное кэширование ML-моделей (Service Worker через VitePWA).
- Исправлено предупреждение IPv6 от `express-rate-limit` в `server.ts` путем использования `ipKeyGenerator`.
- `INFO: Created TensorFlow Lite XNNPACK delegate for CPU` — это стандартное информационное сообщение от библиотеки mediapipe/tensorflow-lite, оно означает, что модуль обработки изображений успешно инициализирован на CPU. Это не является ошибкой и не мешает работе приложения.

**Реализованный ранее функционал:**
1. Настроен бэкенд на Node.js (Express) + Vite (для SPA), порт 3000. Включен production-режим сборки (`server.ts` и `npm run build`).
2. Telegram интеграция: реализовано хранилище для Telegram (отправка логов, сохранение истории и пресетов в CloudStorage), а также `telegramValidationMiddleware` в `server.ts`.
3. История генераций: исправлена утечка памяти (Blob URLs), теперь используются стабильные Data URL, если необходимо, или локальные URL, которые корректно очищаются. Код логики работы с историей находится в `src/services/localHistory.ts`.
4. Компоненты UI: `DailyRewardModal` для ежедневного бонуса, `HistoryCarousel` для просмотра истории образов, `ImageSlider` для сравнения до/после.
5. Интеграция PWA (`src/components/PWAPrompt.tsx`): есть возможность установки приложения на телефон, включая iOS-инструкцию (через "Поделиться" -> "На экран Домой").

**Напоминания для будущих сессий:**
- Всегда читай `STATE.md` при начале новой сессии, чтобы восстановить контекст (согласно правилу в `AGENTS.md`).
- Вся логика Telegram-веб-приложения находится в `telegram-web-app.js`, а бэкенд-маршрутизация — в `server.ts` и `src/server/routes/`.
- Для UI используем Tailwind CSS, React 18 (Vite).
- Взаимодействие с AI-бэкендом ограничено через `express-rate-limit`, чтобы избежать перерасхода квоты.

- Заменены изображения в слайдере на женские (До/После). Использованы статичные сгенерированные изображения для моментальной загрузки и согласованности.
- Добавлены новые женские стрижки в библиотеку (`haircutLibrary.ts`).
- Исправлено центрирование слайдера на главной странице.
- Проведена оценка стоимости генерации библиотеки (fal.ai flux/dev). Расход составил около $0.75-$1.00. Результаты надежно закэшированы в Firestore на 30 дней.
- Подтверждена архитектура VTON: используется fal-ai/face-swap, что гарантирует 100% перенос стрижки (за счет использования референса как базового изображения) и максимальное сходство лица.
- Проведена оценка стоимости генерации библиотеки (fal.ai flux/dev). Расход составил около $0.75-$1.00. Результаты надежно закэшированы в Firestore на 30 дней.
- Подтверждена архитектура VTON: используется fal-ai/face-swap, что гарантирует 100% перенос стрижки (за счет использования референса как базового изображения) и максимальное сходство лица.- Исправлена критическая ошибка в определении пола (`includes('male')` перехватывал `female`), из-за которой женщинам генерировались мужские модели для референсов стрижек. Версия кэша для референсов обновлена, чтобы очистить некорректные изображения.

**Обновление (06 Июля 2026):**
- Пользователь попросил вернуть идеально центрированный стартовый слайдер на главной странице, который мы генерировали ранее (девушка с каштановыми волосами и симметричным лицом).
- Мы пересоздали изображения `split-left.jpg` и `split-right.jpg` с идеальной симметрией через `generate_aligned.cjs` и применили скрипт `blend_seam4.cjs`, который ретуширует (интерполирует) вертикальную темную полосу (seam/артефакт по центру) вместо простого размытия.
- В `ImageSlider.tsx` были обновлены версии параметров `?v=113` для сброса кеша браузера, чтобы новая отретушированная версия применилась.
- Контекст будет сохраняться в этом файле `STATE.md`, как и просил пользователь, чтобы ничего не терять.
- [2026-07-06] Заретуширована полоса на финальном слайдере (BeforeAfterSlider.tsx) путем установки object-cover и scale-[1.02], а также добавлен watermark hider.
- [2026-07-06] Исправлен баг в pdfExport.ts, где html2canvas неправильно рендерил flex-grid для изображений, растягивая их на целые страницы. Теперь используется table-layout, что решает проблему гигантских лиц на страницах PDF.
- [2026-07-06] Исправлено: Гайд PDF теперь использует display: table и table-layout: fixed для сетки фотографий, а ширина колонок жестко задана в 33.33%. Это гарантирует правильное отображение 3-х колонок с фото при генерации через html2canvas (ошибка с гигантскими не вписывающимися фото решена).
- [2026-07-06] Исправлено: Слайдер финальной генерации теперь использует скрытое изображение (hidden img) для автоматического масштабирования контейнера под пропорции сгенерированного фото (afterImage). Внутри слайдера для обоих изображений установлено object-contain. Теперь исходное и сгенерированное фото полностью помещаются в карточку без обрезки и отцентрированы.
- [2026-07-06] Fixed giant images in PDF export by switching from display: table to inline-block layout for html2canvas compatibility.
- [2026-07-06] Fixed BeforeAfterSlider layout blowing up vertically by applying a strict aspect-[3/4] ratio to the container and using object-cover for images.
