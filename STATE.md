# Текущее состояние проекта (НейроСтилист AI)

## Что было сделано:
1. **Биллинг и Ошибка Авторизации**: Исправлена логика в `billing.ts`. Ранее пользователи, у которых в базе не был записан `tgId` (созданные до введения обязательной привязки к Telegram), получали `AUTHORIZATION_ERROR`, из-за чего интерфейс писал "не хватает кредитов/звезд" даже если на балансе было 999. Теперь старым пользователям автоматически прописывается `tgId`.
2. **Ежедневный Бонус**: Исправлен баг, из-за которого ежедневный бонус (+1 генерация) добавлялся только визуально в локальное хранилище браузера, но не синхронизировался с сервером Firebase. Теперь сервер получает токен.
3. **Камера в Telegram**: В `usePhotoHandlers.ts` убрана жесткая проверка мобильного устройства в Telegram, из-за которой кнопка "Сделать фото" принудительно открывала файловый менеджер (который на iOS мог открывать только галерею). Теперь приложение сначала пытается запросить встроенную камеру через `navigator.mediaDevices.getUserMedia`.
4. **Фотографии Топ Стрижек**: Изображения в карусели были настроены на локальные пути. Если они не отображались после экспорта в GitHub, вероятно, экспорт произошел до того, как папка `public/presets` была полностью синхронизирована платформой. После нового экспорта все должно заработать.
5. **Экспорт в PDF**: Картинка исходного фото теперь загружается через прокси для обхода CORS, поэтому она попадает в PDF.
6. **Исправление ошибок загрузки чанков**: Добавлен авторефреш при ошибке загрузки динамических чанков, которые могут возникать после деплоя новых версий.
7. **Исправление Nginx 403 HTML**: Убраны ответы с 403 кодом, которые перехватывались nginx, что приводило к ошибке парсинга JSON на клиенте. Теперь сервер отдает 400 Bad Request.
8. **Изображения референсов (FAILED TO FETCH)**: Названия стрижек в карусели "Топ-стрижек месяца" (например, "Френч кроп") не совпадали с названиями в официальной базе библиотеки (там было "French Crop"). Из-за этого срабатывала защита сервера (возвращая 403, перехватываемый Nginx). Имена синхронизированы, теперь все картинки карусели загружаются корректно.
9. **Баг с 999 генерациями**: У разработчиков в интерфейсе появлялось 999 генераций, но сервер выдавал ошибку "Закончились генерации". Причина заключалась в том, что Nginx-прокси вырезал кастомный заголовок `x-developer-mode`. Теперь статус разработчика передается прямо в теле запроса (FormData), обходя ограничения прокси.
10. **Telegram Stories**: Исправлена ошибка "К сожалению, не удалось поделиться в сторис". Проблема была в том, что функция браузера для генерации видео (`canvas.captureStream`) не поддерживается на некоторых версиях iOS/Safari в Telegram. Теперь добавлен безопасный "фолбэк": если создание видео падает с ошибкой, приложение мгновенно создает и отправляет в сторис статичную фотографию в формате До/После (коллаж).
11. **Качество генерации**: Улучшен промпт Gemini (системные инструкции) для предотвращения изменения черт лица у лысых или бритоголовых людей при кардинальной смене прически. Теперь нейросеть получает строгие инструкции не менять изначальное строение головы.
12. **Тестирование Stories**: Улучшена логика предупреждений для тестирования функции "В сторис" вне среды Telegram (например, в песочнице Google Studio). Если кнопка нажимается из обычного браузера, пользователю показывается понятное окно с объяснением.

## Запланировано на следующие сессии (проверка, тесты, баги):
1. **Экспорт**: Пользователю необходимо заново экспортировать проект в GitHub/ZIP, чтобы получить все внесенные изменения и патчи.
