import { Jimp } from 'jimp';

async function run() {
  // Let's use a known before/after image if we can find one.
  // Or we just fetch a good unsplash image and simulate a "before" by blurring it? No, the user wants realistic haircut.
}
run();
