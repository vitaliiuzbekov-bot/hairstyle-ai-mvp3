const fetch = require('node-fetch');

async function search() {
  const url = "https://api.unsplash.com/search/photos?query=pink%20hair%20woman&client_id=YOUR_ACCESS_KEY";
  // I don't have an unsplash key. Let's just use source.unsplash.com ? It's deprecated.
}
